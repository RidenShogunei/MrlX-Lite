# HotpotQA Dynamic MAS

This repository contains our HotpotQA multi-agent RL/SFT experiments.

The original MrlX example projects have been removed from this branch. The
active project lives in:

```text
lightweight_cotrain/
```

Start here:

```text
lightweight_cotrain/README.md
lightweight_cotrain/docs/
```

Current line:

```text
local HotpotQA search/read environment
-> fixed Main/Sub MAS baseline
-> dynamic Main routing with 0..N shared Sub agents
-> Main-only synthesis SFT
-> future Main-only dynamic GRPO
-> future joint GRPO
```
